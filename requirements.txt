# ── Vanna 2.0 with SQLite and Gemini support ──────────────────────────────────
# Use vanna[gemini,sqlite] if you chose Option A (Google Gemini)
# Use vanna[sqlite] for Groq or Ollama
vanna[sqlite]>=2.0.0

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Data + charts ─────────────────────────────────────────────────────────────
plotly
pandas

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv

# ── LLM provider — uncomment the one you chose ───────────────────────────────
# Option A — Google Gemini (also change vanna[sqlite] to vanna[gemini,sqlite] above)
# google-genai

# Option B — Groq (OpenAI-compatible, no extra package needed beyond vanna)
groq
google-generativeai

# Option C — Ollama: install the Ollama desktop app separately, no pip package needed
